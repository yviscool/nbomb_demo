// import { Application } from '@nuke/core';

export default (app:any) => {


  // console.log(app)

  const { controller, router } = app;

  controller && router;

  // router.get('/home', controller.home.index)

};
